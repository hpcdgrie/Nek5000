have this at the first line of your usr code

#include "incompressible_turb_statistic_code/incompressible_turb_statistic_code.usr"


in userchk:

define y_plus_info_dump_freq and 
mesh_size_dump_freq

and call:

     call incompressible_turb_stat(y_plus_info_dump_freq,mesh_size_dump_freq)



